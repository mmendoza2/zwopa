<%= form_for(@actividadpadre) do |f| %>
  <% if @actividadpadre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@actividadpadre.errors.count, "error") %> prohibited this actividad from being saved:</h2>

      <ul>
      <% @actividadpadre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
