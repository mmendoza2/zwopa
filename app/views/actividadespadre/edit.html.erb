<h1>Editing actividad</h1>


<div class="row">
  <div id="formrow">

    <%= form_for(@actividadpadre, :html => { :multipart => true }) do |f| %>
        <%= render 'shared/error_messages', object: f.object %>


        <%= f.label :actividadpadre %>
        <%= f.text_field :actividadpadre %>

        <%= f.label :descripcion %>
        <%= f.text_area :descripcion %>

        <%= f.label :imagen %>
        <%= f.file_field :photo %>

        <%= f.submit "Sube tu actividad", class: "btn btn-large btn-primary" %>

    <% end %>
  </div>
</div>


<%= link_to 'Show', @actividadpadre %> |
<%= link_to 'Back', actividadpadre_path %>
