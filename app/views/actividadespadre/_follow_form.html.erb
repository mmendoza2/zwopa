
    <div id="follow_form">
      <% if current_user.followingactividadpadre?(@user) %>
          <%= render 'unfollow' %>
      <% else %>
          <%= render 'follow' %>
      <% end %>
    </div>
