<%= form_for(current_user.relationestados.build(followed_id: @estado.id), remote: true) do |f| %>
    <div><%= f.hidden_field :followed_id %></div>
    <%= f.submit "Seguir", class: "btn btn-large btn-primary" %>
<% end %>